# VALORANT League Manager 개발 계획

## 현재 목표

지금의 최우선 목표는 기능을 더 많이 추가하는 것이 아니다.

**맵 테스트에서 한 경기를 처음부터 끝까지 관전했을 때 흐름, 결과, 통계와 화면이 모두 납득되는 상태**를 먼저 만든다. 경기 엔진과 상태 구조가 안정된 뒤에 세이브, 성장, 이적 같은 장기 운영 기능으로 넘어간다.

## 작업 원칙

- 문제는 가능하면 같은 seed로 재현한다.
- 맵 테스트의 `CODEX DIAGNOSTIC` 내용을 기준으로 원인을 찾는다.
- 화면에서 이상해 보이는 결과와 엔진 내부 원인을 구분한다.
- 수정할 때마다 기존 자동 검증과 고정 seed 회귀검사를 실행한다.
- 실제 VCT CSV에서 확인할 수 있는 값은 감으로 조정하지 않는다.
- 사용자 경기와 백그라운드 경기는 같은 핵심 엔진을 사용한다.
- 계산 규칙, 밸런스 설정, 데이터와 UI 표현을 계속 분리한다.

## 문제 수정 흐름

1. 맵 테스트에서 문제가 발생한 경기를 끝까지 진행한다.
2. `진단 내용 복사`로 JSON 보고서를 가져온다.
3. 문제 장면과 함께 진단 내용을 Codex에 전달한다.
4. seed, 라운드, 경제, 전술, 이벤트와 공간 좌표로 원인을 재현한다.
5. 엔진 또는 UI를 수정한다.
6. 같은 seed에서 문제가 사라졌는지 확인한다.
7. 다른 seed에서도 부작용이 없는지 자동 검증한다.

## 1단계: 중계 화면 안정화

한 경기의 모든 정보를 한 화면에서 읽을 수 있게 한다.

- 1440px 경기 화면과 작은 화면에서 잘림·겹침을 제거한다.
- 상단 스코어, 맵 선택 정보, 진영과 라운드 시간을 정리한다.
- 좌우 선수 HUD의 요원, 체력, 아머, 무기, 스킬, ULT와 K/D를 안정적으로 표시한다.
- 맵 위 해설 토스트가 중요한 사건을 적절한 시간 동안 보여주게 한다.
- 일시정지, 계속 진행과 한 라운드 진행이 정확히 동작하게 한다.
- 라운드 종료 화면의 24칸 진행표, 공수 교대와 연장 칸을 정리한다.
- 결과 화면의 크레딧, 장비, 스킬, KDA와 선수 정보가 겹치지 않게 한다.
- 한글·영어 전환 시 폭과 높이가 변해도 UI가 깨지지 않게 한다.

완료 조건:

- 일반적인 데스크톱 화면에서 경기 전체가 한눈에 들어온다.
- 양 팀 어느 쪽에서도 텍스트, ULT, KDA와 이미지가 겹치지 않는다.
- 정규 24라운드와 연장 진행표가 올바르게 늘어난다.

## 2단계: 라운드 사건의 논리 검증

구매부터 라운드 종료까지 사건 순서가 실제 경기처럼 이어지게 한다.

- 구매, 이동, 유틸리티, 첫 교전, 트레이드, 설치, 해체와 종료 순서를 검증한다.
- 사망한 선수가 이후 킬, 스킬 사용, 설치 또는 해체를 하지 못하게 한다.
- 같은 선수가 한 라운드에서 중복 사망하지 않게 한다.
- 설치 없는 해체, 사망 이후 해체와 시간 종료 이후 사건을 차단한다.
- 킬 수와 생존자 수, 승리 팀과 종료 원인이 일치하게 한다.
- 어시스트, 첫 킬·첫 데스, 트레이드와 클러치 판정을 검증한다.
- 타이머와 스파이크 시간이 사건 로그와 일치하게 한다.

완료 조건:

- 진단 패널의 사건 논리 오류가 0건이다.
- 고정 seed 회귀검사에서 같은 라운드 사건과 결과가 나온다.

## 3단계: 맵 이동과 교전 정밀화

각 맵의 공간 구조가 실제 라운드 결과에 의미 있게 작용하도록 만든다.

- 공격·수비 스폰과 라운드 시작 위치를 검증한다.
- 벽 통과, 맵 외부 좌표와 비정상 순간이동을 제거한다.
- A·B 사이트 진입로, 미드, 연결로와 로테이션 경로를 보정한다.
- 전술에 따라 진입 경로와 수비 배치가 달라지게 한다.
- 시야 방향, 엄폐, 크로스파이어와 교전 거리를 반영한다.
- 설치 가능 구역과 해체 접근 경로를 검증한다.
- 일곱 맵 모두 같은 진단 기준을 통과하게 한다.

완료 조건:

- 모든 맵에서 좌표 이탈과 벽 통과가 없다.
- 선수 이동만 보아도 선택한 사이트와 전술을 이해할 수 있다.
- 교전이 주요 진입로와 사이트 주변에서 자연스럽게 발생한다.

## 4단계: 경제와 장비 완성

경제 상태가 라운드 선택과 승률에 자연스럽게 영향을 주게 한다.

- 피스톨, 에코, 포스, 보너스, 하프바이와 풀바이를 구분한다.
- 팀 크레딧과 패배 보너스를 정확히 이월한다.
- 무기, 아머와 스킬 가격을 적용한다.
- 선수와 역할에 맞는 구매 우선순위를 만든다.
- 다음 라운드를 고려한 세이브 판단을 구현한다.
- 구매한 장비와 중계·결과 화면의 표시가 일치하게 한다.
- 구매 유형별 승률이 지나치게 평평하거나 극단적이지 않은지 검증한다.

완료 조건:

- 크레딧이 음수가 되거나 구매 결과가 불일치하지 않는다.
- 에코, 포스와 풀바이 라운드의 차이가 경기에서 체감된다.

## 5단계: 전술과 감독 개입 완성

감독의 선택이 다음 라운드에 설명 가능한 영향을 주게 한다.

- 공격 사이트, 템포, 기본 운영과 진입 방식을 세부 전술로 분리한다.
- 수비 배치, 적극성, 리테이크와 스택 판단을 구현한다.
- 상대가 반복하는 전술을 인식하고 대응하게 한다.
- 타임아웃에서는 다음 라운드부터 전술을 변경한다.
- 정규 구간 팀별 2회, 연장 구간별 1회 규칙을 유지한다.
- AI 감독도 사용자와 같은 정보와 규칙을 사용한다.
- 전술 변경 전후의 결과를 진단 보고서에서 비교할 수 있게 한다.

완료 조건:

- 같은 전력에서도 전술 선택에 따라 이동과 라운드 양상이 달라진다.
- 전술 효과가 결과를 확정하지 않고 유의미한 확률 차이만 만든다.

## 6단계: 경기 통계와 결과 검증

표시되는 모든 기록을 라운드 원천 이벤트에서 계산한다.

- K, D, A, FK, FD, 트레이드, 플랜트, 해체와 클러치를 검증한다.
- ACS, ADR, KAST, KPR, APR, K:D, FK:FD와 Rating을 재검증한다.
- 맵별 합계와 경기 박스스코어가 일치하게 한다.
- 경기 결과를 대회, 연도와 선수 역사에 연결한다.
- 선발 라인업의 이번 대회 Rating이 경기 종료 직후 갱신되게 한다.
- 원천 이벤트와 박스스코어 불일치를 진단 패널에서 자동 감지한다.

완료 조건:

- 사건 로그를 다시 집계한 값과 화면 통계가 일치한다.
- 같은 선수 ID로 실제 역사와 게임에서 생성된 역사가 이어진다.

## 7단계: 실제 VCT 분포 기반 밸런스 조정

경기 논리와 통계가 안정된 뒤 대규모 표본으로 밸런스를 맞춘다.

- `stats/**/*.csv`에서 실제 기준 분포를 생성한다.
- 최소 10,000경기의 고정 seed 시뮬레이션 표본을 만든다.
- 실제와 시뮬레이션의 다음 항목을 비교한다.
  - 맵당 평균 라운드와 스코어 차이
  - 연장 비율과 업셋 비율
  - 공격·수비와 맵별 승률
  - 피스톨, 에코, 보너스, 포스와 풀바이 승률
  - KPR, APR, ADR, KAST, ACS, K:D와 FK:FD
  - 멀티킬, 트레이드와 클러치 빈도
  - 역할군과 요원별 기록 분포
- 평균 맵 길이 16.53라운드, 연장 0%, 열세 팀 업셋 0%로 확인된 기존 편향을 우선 수정한다.
- 설정값 변경 전후를 같은 seed 보고서로 비교한다.

완료 조건:

- 강팀이 장기적으로 우세하지만 단일 경기의 이변 가능성이 존재한다.
- 지나치게 짧은 경기와 일방적인 연속 킬이 줄어든다.
- 실제 VCT 분포와의 차이가 허용 범위 안에 들어온다.

## 한 경기 완성 판정

다음 조건을 모두 만족하면 경기 기반이 완성된 것으로 본다.

- 서로 다른 seed의 경기 10개 이상을 처음부터 끝까지 관전한다.
- 치명적인 사건 오류, 좌표 이탈과 UI 겹침이 없다.
- 경제, 전술과 선수 능력의 영향이 화면에서 설명된다.
- 라운드 흐름과 최종 스코어가 납득된다.
- 결과 통계가 원천 이벤트와 일치한다.
- 같은 seed를 다시 실행하면 같은 결과가 나온다.
- 한 경기를 끝까지 관전하는 과정이 반복 테스트가 아니라 게임처럼 느껴진다.

## 경기 완성 이후 진행 순서

1. 버전이 있는 JSON 세이브와 불러오기
2. 매 경기 출전 5인 선택과 로테이션
3. 폼, 컨디션, 피로와 사기
4. 훈련, 성장, 노화와 능력치 변화
5. 코칭스태프와 장기 전술 숙련
6. 계약, 영입, 이적, 방출과 예산
7. Kickoff, Stage, Masters와 Champions를 연결한 연간 일정
8. 시즌 종료, 연도 전환과 다음 시즌 로스터 변화

진행 중인 경기 저장은 경기 상태 구조가 안정된 뒤 추가한다. 데이터 규모와 동시 접근 요구가 커질 때만 SQLite 전환을 다시 검토한다.
