# VALORANT League Manager 개발 계획

## 현재 목표

기능을 넓히기 전에 **한 맵을 처음부터 끝까지 봤을 때 선수 10명의 움직임과 판단이 논리적으로 이해되는 상태**를 만든다.

모든 움직임은 다음 질문에 답할 수 있어야 한다.

- 이 선수는 지금 무엇을 알고 있는가?
- 누구에게 어떤 정보를 전달받았는가?
- 현재 IGL 명령과 개인 임무는 무엇인가?
- 왜 이동·정지·교전·스킬 사용을 선택했는가?
- 팀원이 죽거나 새로운 정보가 들어왔을 때 판단이 어떻게 바뀌었는가?

설명할 수 없는 움직임은 버그 또는 미완성 판단으로 취급한다. 바로 고치기 어렵다면 이 문서의 `관찰된 문제`에 기록한다.

## 작업 원칙

1. 같은 seed로 문제를 재현한다.
2. 감독 모드의 팀 음성과 `CODEX DIAGNOSTIC`을 함께 확인한다.
3. 콜이 잘못됐는지, 콜은 맞지만 수행이 잘못됐는지 구분한다.
4. 위치·시야·발소리·스킬·교전처럼 물리적인 사실과 AI의 추정을 구분한다.
5. 한 문제를 고친 뒤 고정 seed와 여러 무작위 seed를 모두 검증한다.
6. 결과 수치를 억지로 맞추기 전에 라운드 과정이 자연스러운지 확인한다.

## 최우선: 한 경기의 논리 완성

### 1. 정보와 발소리 모델

- 발소리는 단순한 사이트 정보가 아니라 `출발 구역 → 도착 구역`으로 기록한다.
- 접근, 이탈, 횡이동, 거리, 마지막 청취 시각과 신뢰도를 구분한다.
- 오래된 발소리는 시간에 따라 신뢰도가 감소한다.
- 여러 선수가 같은 소리를 들었을 때 중복 보고를 하나의 팀 정보로 합친다.
- 발소리가 끊겼다고 해당 위치에 적이 확정적으로 있다고 판단하지 않는다.
- 가짜 발소리, 멈춤, 다시 걷기 때문에 이동 방향 추정이 틀릴 가능성을 둔다.
- 수비 로테이션은 발소리 방향과 기존 사이트 배치를 함께 평가한다.

완료 조건:

- `B 메인 → B 지점`과 `B 메인 → 공격 진영` 소리에 서로 다른 반응을 보인다.
- 진단에서 각 로테이션을 유발한 소리의 경로를 확인할 수 있다.

### 2. 개인 판단과 팀 규율

- IGL 명령은 목표와 제약을 정하고, 선수는 매 틱 자신의 행동을 판단한다.
- 규율, 판단력, 팀워크, 침착성에 따라 명령 수행 지연과 이탈 확률을 다르게 한다.
- 선수는 적 발견, 발소리, 스킬 준비, 지원 거리, 위험한 각도를 IGL에게 보고한다.
- 잘못된 개인 제안도 가능하지만 IGL이 승인·거절·보류한다.
- 팀원 사망, 스파이크 드롭, 숫자 우위 변화가 기존 홀드 명령보다 높은 우선순위를 가질 수 있다.
- 명령이 바뀌어도 현재 교전·엄폐·스킬 시전 상태를 무시하고 순간이동하듯 전환하지 않는다.

완료 조건:

- 같은 전술에서도 선수 능력과 성향에 따른 실행 차이가 보인다.
- 모든 중요한 이동에는 최근 명령과 개인 판단 근거가 남는다.

## 관찰된 문제 목록

- [ ] 전체 이동 속도와 의사결정 사이에 답답한 공백이 있음
- [ ] 비어센트 맵의 문·계단·텔레포터·로프 등 특수 지형은 맵별 수동 정밀 교정이 필요함
- [ ] 전멸 승리가 실제 65.0%보다 높은 81.0%이고 해체 승리는 실제 20.7%보다 낮은 9.9%로, 단계형 리테이크와 생존 판단을 더 교정해야 함
- [ ] ADR은 실제 121.2보다 낮은 94.6이며, 다중 탄환 구조 이후에도 차이가 남아 미구현 요원 피해·회복·지속 피해를 실제 규칙대로 확장해야 함

문제를 새로 발견하면 이 목록에 먼저 기록하고, 수정 후 고정 seed 재현과 다중 seed 회귀 검사를 통과했을 때 체크한다.

## 당장 진행할 순서

1. 단계형 리테이크·재진입·해체 보호 판단 검증 및 고도화
2. 요원별 피해·회복·지속 피해 스킬 실측 감사와 구현
3. 비어센트 맵 특수 지형 수동 교정

## 한 경기 완성 판정

- 서로 다른 seed의 경기 10개 이상을 처음부터 끝까지 관전한다.
- 설명할 수 없는 이동, 전원 동시 전환, 치명적인 시간 오류가 없다.
- 콜, 명령, 실제 행동과 결과가 하나의 타임라인으로 연결된다.
- 같은 seed를 다시 실행하면 같은 경기와 진단 결과가 나온다.
- 전술과 선수 특성이 결과를 강제하지 않으면서도 체감 가능한 차이를 만든다.
- 한 경기를 관전하는 과정이 반복 테스트가 아니라 실제 게임처럼 느껴진다.
