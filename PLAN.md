# VALORANT League Manager 개발 계획

이 문서는 현재까지 구현된 기반과 앞으로 진행할 작업을 구분한다. 목표는 실제 VCT 데이터를 출발점으로 삼되, 장기간 진행하면서 새로운 경기 기록과 선수 역사가 축적되는 매니지먼트 게임을 만드는 것이다.

## 현재 구현된 기반

### 데이터와 로스터

- 2021~2026 VCT CSV를 ID 기반 JSON/JSONL 구조로 변환한다.
- 선수, 팀, 대회, 요원, 맵과 연도별 기록을 서로 연결한다.
- 2026 국제 리그 로스터를 게임 런타임 데이터로 생성한다.
- `runtime_2026/roster-overrides-2026.json`에서 로스터를 수동으로 교정할 수 있다.
- 원본, 프로필, 능력치 계산 결과와 게임 런타임 데이터를 분리한다.

### 선수 평가

- Firepower, Combat Efficiency, Entry, Positioning, Teamplay, Tactical, Clutch, Explosiveness, Consistency, Adaptability, Pressure를 사용한다.
- 역할별 OVR, 역할 숙련도, 요원 숙련도와 맵 경험을 지원한다.
- 여러 시즌의 기록과 표본 신뢰도를 반영한다.
- 능력치 계산 규칙과 생성 스크립트를 데이터에서 분리해 관리한다.

### 경기 시뮬레이션

- 같은 seed에서 같은 결과가 나오는 재현 가능한 RNG를 사용한다.
- 경기, 맵과 라운드 seed를 분리한다.
- Bo1, Bo3, Bo5와 형식별 맵 비토를 지원한다. 리그 기본 형식은 Bo3이다.
- 요원 조합, 역할 균형, 선수 능력치, 진영, 경제와 전술 성향을 경기 계산에 사용한다.
- 라운드 페이즈, 공간 이동, 첫 교전, 트레이드, 크로스파이어, 설치, 해체와 클러치를 계산한다.
- 맵별 지형과 내비게이션 데이터를 사용한다.
- UI 없는 빠른 경기와 사용자 관전 경기가 같은 핵심 엔진을 사용한다.
- 사용자 관전 경기는 맵 전체를 선계산하지 않고 인메모리 상태에서 한 라운드씩 진행한다.

### UI

- 팀 선택과 팀·선수 상세 화면
- 한글·영어 전환
- 맵 비토와 요원 드래프트
- 전술 성향 선택
- 경기 중계와 라운드 피드
- 선수 역사와 대회 통계
- 맵 실험 화면

### 자동 검증

- seed 재현성
- Bo1·Bo3·Bo5 경기 형식
- 일괄 시뮬레이션과 라운드별 시뮬레이션의 일치
- 능력치와 경험 민감도
- 요원 조합, 경제, 전술, 맵, 라운드 페이즈와 트레이드
- 다수 경기의 평균 라운드, 연장, 업셋, Rating과 K/D 분포

## 다음 개발 단계

### 1. 경기 중 감독 개입

현재 인메모리 라운드 상태를 이용해 라운드 사이에 감독이 개입할 수 있게 한다.

현재 완료: 자동 진행 정지·재개, 한 라운드 진행, 사용자 타임아웃 전술 편집, AI 타임아웃 판단, 정규·연장 횟수 검증과 로그 저장.

- 자동 진행, 일시정지, 한 라운드 진행과 빠른 진행을 구분한다.
- 라운드가 끝난 시점에만 전술 변경을 허용한다.
- 경기 시작 때 정한 공격·수비 성향과 세부 전술을 다시 조정할 수 있게 한다.
- 변경된 전술은 이미 끝난 라운드가 아니라 다음 라운드부터 적용한다.
- AI 감독도 같은 규칙과 전술 모델을 사용한다.

#### 타임아웃 규칙

- 정규 구간에서 팀마다 공격·수비 진영을 합쳐 총 2회를 사용할 수 있다.
- 진영이 바뀌어도 남은 정규 타임아웃 횟수는 초기화되지 않는다.
- 타임아웃은 경기 시작 또는 이전 타임아웃에서 정했던 전술 성향을 변경하는 공식 기회다.
- 연장에 들어가면 연장 구간마다 팀별 타임아웃 1회를 추가로 사용할 수 있다.
- 여기서 연장 구간은 양 팀이 공격과 수비를 한 번씩 수행하는 2라운드 묶음을 뜻한다.
- 다음 연장 구간으로 넘어가면 해당 구간용 타임아웃이 다시 1회 주어진다. 남은 연장 타임아웃은 다음 구간으로 이월하지 않는다.
- 타임아웃 사용 여부와 변경 전·후 전술은 경기 로그에 기록한다.

완료 조건:

- 화면에서 남은 타임아웃 횟수를 확인할 수 있다.
- 정규 구간과 연장 구간의 횟수 규칙이 자동 검증된다.
- 타임아웃에서 바꾼 전술이 다음 라운드 계산에 반영된다.
- 빠른 경기에서도 AI의 타임아웃과 전술 변경을 동일한 규칙으로 처리한다.

### 2. 생성 경기 통계 완성

현재 완료: 교전 이벤트 기반 피해량, ACS, ADR, KAST, KPR, APR, K:D, FK:FD와 종합 Rating의 경기 박스스코어 집계 및 자동 검증.

확인된 후속 과제: 30경기 진단 표본에서 평균 맵 길이 16.53라운드, 연장 0%, 전력상 열세 팀의 업셋 0%가 나왔다. 이는 통계 산식이 아니라 경기 승률과 연속 처치 분배의 기존 편향이므로 7단계 밸런스 조정에서 우선 수정한다.

- 시뮬레이션 이벤트로 ACS, ADR, KAST, K:D, KPR, APR과 FK:FD를 계산한다.
- K, D, A, FK, FD, 플랜트, 해체, 클러치와 트레이드를 누적한다.
- 맵별, 경기별, 대회별, 연도별 집계를 만든다.
- 기존 VCT 역사와 게임에서 새로 생성된 역사를 같은 선수 ID로 연결한다.
- Rating은 원천 이벤트에서 계산하며 표시용 ACS를 능력치 계산에 중복 사용하지 않는다.

### 3. 시즌과 대회 구조

현재 완료: 정규시즌 일정의 안정적인 경기 ID, 사용자·백그라운드 경기의 단일 결과 반영 함수, 경기별 선수 박스스코어 보관, 승수·맵 득실·라운드 득실·상대전적 순위 결정 기반.

현재 완료: 2026 VCT Stage를 참고하되 알파·오메가 조를 제거한 12팀 단일 리그전, 상위 8팀 더블 엘리미네이션, 패자조 결승·결승 Bo5, 승자·패자 진출선 자동 연결, 우리 팀 일정·전체 일정·대진표 화면과 독립 대회 정보 객체.

- 정규시즌 일정과 순위 결정 규칙을 완성한다.
- Kickoff, Stage, 플레이오프와 국제대회 대진을 구현한다.
- Masters와 Champions 진출 조건을 연결한다.
- 시즌 종료, 연도 전환과 다음 시즌 로스터 상태를 만든다.
- 경기 결과가 순위, 대진과 선수 역사에 즉시 반영되게 한다.

### 4. 세이브와 불러오기

첫 단계에서는 별도 DB 없이 버전이 있는 JSON 세이브를 사용한다.

- 현재 날짜·주차, 일정, 순위와 대회 상태
- 팀 로스터와 선발 라인업
- 선수 능력치, 숙련도, 폼, 컨디션과 누적 기록
- 경기 결과와 중요한 라운드 로그
- 난수 seed와 게임 버전
- 저장 중단에 대비한 임시 파일 후 교체 방식
- 구버전 세이브를 읽기 위한 마이그레이션 구조

진행 중인 경기 저장은 시즌 저장 구조가 안정된 뒤 추가한다. 데이터 규모나 동시 접근 요구가 커질 때만 SQLite 전환을 다시 검토한다.

### 5. 선수와 팀 관리

- 훈련, 성장, 노화와 능력치 하락
- 경기 폼, 컨디션, 피로와 사기
- 출전 경험에 따른 역할·요원·맵 숙련도 변화
- 매 경기 출전 5인 선택과 로테이션
- 코칭스태프 효과
- 계약, 영입, 이적, 방출과 예산

### 6. AI 감독

- 상대, 맵, 선수 상태를 고려한 선발 5인 선택
- 요원 조합과 시작 전술 선택
- 스코어, 경제, 상대 패턴을 고려한 타임아웃 판단
- 타임아웃 중 전술 변경
- 시즌 중 로테이션과 장기 선수 관리

AI가 사용자와 다른 숨겨진 보너스를 받지 않고 같은 평가 함수와 규칙을 사용하게 한다.

### 7. 시뮬레이션 정밀 조정

- 최소 10,000경기 단위의 고정 seed 회귀 검증
- 평균 라운드 수와 연장 비율
- 전력 차가 있는 경기의 업셋 비율
- 역할별 Rating, K/D, ADR, KAST와 첫 교전 분포
- 공격·수비와 맵별 승률
- 능력치, 전술과 조합 효과의 과대·과소 여부
- 실제 VCT 분포와 게임 분포 비교

#### 실제 CSV 기반 기준선

밸런스 수치는 감이나 특정 유명 선수의 체감으로 결정하지 않는다. `stats/**/*.csv`에 축적된 실제 VCT 경기·맵·이벤트 표본에서 다음 기준 분포를 먼저 생성한다.

- 맵당 평균 라운드, 스코어 차이, 연장과 업셋 비율
- 선수별 KPR, APR, ADR, KAST, ACS, K:D와 FK:FD
- 멀티킬과 한 선수가 한 라운드에서 가져가는 연속 처치 빈도
- 역할군별 통계 분포와 요원별 차이
- 공격·수비, 피스톨, 에코, 보너스와 풀바이 승률
- 맵, 리그, 대회 등급과 시즌별 환경 차이
- 팀 전력 차이에 따른 실제 승리 확률 곡선

검증 과정은 `실제 CSV 기준 분포 생성 → 같은 조건의 시뮬레이션 표본 생성 → 차이 보고서 작성 → 설정값 조정 → 고정 seed 회귀검사` 순서로 자동화한다. 원본 CSV가 제공하지 않는 값은 추정치와 실제 관측치를 구분해 표시한다. 기준 분포와 시뮬레이션 보고서는 재생성 가능한 스크립트와 버전이 있는 설정 파일로 보존한다.

특정 유명 선수의 체감 순위에 억지로 맞추기보다 전체 분포와 입력 데이터의 설명력을 우선한다.

### 8. 맵 정밀화

- 맵별 스폰, 사이트, 주요 진입로와 교전 지점 보정
- 사이트별 공격·수비 난이도
- 로테이션 거리와 시간
- 전술·요원 조합과 맵 구조의 상호작용
- 중계 화면의 위치와 이동 자연스러움 개선

## 권장 구현 순서

1. 경기 중 정지·진행 제어와 타임아웃
2. 경기 이벤트 기반 통계 완성
3. 시즌·대회 진행
4. JSON 세이브·불러오기
5. 선수 성장과 팀 관리
6. AI 감독 고도화
7. 대규모 밸런스 조정과 맵 정밀화

## 개발 원칙

- 원본 데이터와 계산 결과를 분리한다.
- 바뀔 수 있는 명칭은 안정적인 ID로 연결한다.
- 계산 규칙, 밸런스 수치와 UI 표현을 분리한다.
- 사용자 경기와 백그라운드 경기는 같은 핵심 엔진을 사용한다.
- 새로운 능력치나 계산 방식이 추가돼도 기존 모듈 전체를 다시 작성하지 않도록 경계를 유지한다.
- 불확실한 데이터는 임의로 확정하지 않고 검증 보고서나 격리 데이터로 남긴다.
- 같은 seed로 변경 전후 결과를 비교할 수 있게 한다.
